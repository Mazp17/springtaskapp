package com.miguel.TaskAppBackend;

import org.springframework.boot.CommandLineRunner;
import org.springframework.stereotype.Component;

@Component
public class TaskAppCommand implements CommandLineRunner {

    @Override
    public void run(String... args) throws Exception {
    /*
        Task newTask = new Task(null,
                "first Task",
                "First task created by spring framework in the backend",
                false);
        Task save = taskService.save(newTask);
        System.out.println(save.toString());

    */
        /*Optional<User> miguel = userDAO.findById(1);

        Iterable<Task> tasks*/

        /*User miguel = new User(null,
                "miguel",
                "miguel123",
                "miguel@zapata.com");
        User save = service.save(miguel);
        System.out.println(save.toString());*/
        /*Task task = new Task(null,d
                "primera nota",
                "primera nota creada mediante spring",
                false);
        Task save = service.save(task);
        System.out.println(save.toString());*/
    }
}
